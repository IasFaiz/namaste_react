import React from "react";
import ReactDOM from "react-dom/client";

const Heading = "Hello, React!";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<h1>{Heading}</h1>);
